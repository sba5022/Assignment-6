import React from 'react';

const SelectedPro = ({selectedProducts}) => {
    console.log(selectedProducts,'selected products')
    return (
        <div>
            selected products
        
        </div>
    );
};

export default SelectedPro;